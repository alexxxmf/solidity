overwrite: true
schema: "./schema.graphql"
generates:
  ./generated/types.ts:
    plugins:
      - add: "/* tslint:disable */"
      - typescript
      - typescript-resolvers
    config:
      avoidOptionals: true
